import { styled } from "../../stitches.config";

const Styles = styled("a", {
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  width: "60rem",
  height: "60rem",
  borderRadius: "60rem",
  background: "#0F0000",
  position: "fixed",
  right: "60rem",
  bottom: "60rem",
});

export default Styles;
